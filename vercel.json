{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm turbo run build --filter=@rebel/ui...",
  "outputDirectory": "apps/ui/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
